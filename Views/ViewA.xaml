<UserControl x:Class="PSamples.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <Label Content="画面A"
               FontSize="30"
               Foreground="Brown">
            </Label>
            <TextBlock></TextBlock>
            <Label Content="{Binding MyLabel}"></Label>
            <Button Content="OK"
                    Command="{Binding OKButton}"></Button>
            <Button Content="OK2"
                    Command="{Binding OKButton2}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
